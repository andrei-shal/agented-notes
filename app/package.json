{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate && bun run db/fts5.ts",
    "check": "tsc --noEmit",
    "test": "bun test --isolate"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "drizzle-orm": "^0.38.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "jose": "^5.9.0",
    "zod": "^3.24.0",
    "rrule": "^2.8.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "better-sqlite3": "^13.0.1",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0"
  }
}
